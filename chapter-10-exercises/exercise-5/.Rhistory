# Read in the female baby names data file found in the `data` folder into a
# variable called `names`. Remember to NOT treat the strings as factors!
names = read.csv("data\female_names.csv")
# Read in the female baby names data file found in the `data` folder into a
# variable called `names`. Remember to NOT treat the strings as factors!
names = read.csv("data/female_names.csv")
view(names)
View(names)
# Read in the female baby names data file found in the `data` folder into a
# variable called `names`. Remember to NOT treat the strings as factors!
names = read.csv("data/female_names.csv", stringsAsFactors = FALSE)
# Create a data frame `names_2013` that contains only the rows for the year 2013
names_2013 = names[names$year == 2013]
# Create a data frame `names_2013` that contains only the rows for the year 2013
names_2013 = names[names$year == 2013]
# Create a data frame `names_2013` that contains only the rows for the year 2013
names_2013 = names[names$year == "2013"]
# Create a data frame `names_2013` that contains only the rows for the year 2013
names_2013 = names[names[names$year == "2013"]]
names[names$year == "2013"]
names$year
names$year == 2013
# Create a data frame `names_2013` that contains only the rows for the year 2013
names_2013 = names[names$year == 2013]
# Create a data frame `names_2013` that contains only the rows for the year 2013
names_2013 = names[names$year == names$year[names$year == 2013]]
# Read in the female baby names data file found in the `data` folder into a
# variable called `names`. Remember to NOT treat the strings as factors!
names = read.csv("data/female_names.csv", stringsAsFactors = FALSE)
# Create a data frame `names_2013` that contains only the rows for the year 2013
names_2013 = names[names$year == names$year[names$year == 2013]]
# Create a data frame `names_2013` that contains only the rows for the year 2013
names_2013 = names[names$year[names$year == 2013]]
names$year[names$year == 2013]
# Create a data frame `names_2013` that contains only the rows for the year 2013
names_2013 = subset(names,[names$year[names$year == 2013]])
# Create a data frame `names_2013` that contains only the rows for the year 2013
names_2013 = subset(names,(names$year[names$year == 2013]])
# Create a data frame `names_2013` that contains only the rows for the year 2013
names_2013 = subset(names,(names$year[names$year == 2013])
# Create a data frame `names_2013` that contains only the rows for the year 2013
names_2013 = subset(names,names$year[names$year == 2013])
source('D:/YifusDocuments/Master/DataScience/Data_Processing/book-exercises/chapter-10-exercises/exercise-5/exercise.R')
# Create a data frame `names_2013` that contains only the rows for the year 2013
names_2013 = subset(names,names$year == 2013)
View(names_2013)
?count
count
?n
table(names_2013)
table(names_2013$name)
# What was the most popular female name in 2013?
tname = table(names_2013$name)
source('D:/YifusDocuments/Master/DataScience/Data_Processing/book-exercises/chapter-10-exercises/exercise-5/exercise.R')
t = as.data.frame(table(tname))
t
tname
max(tname)
source('D:/YifusDocuments/Master/DataScience/Data_Processing/book-exercises/chapter-10-exercises/exercise-5/exercise.R')
source('D:/YifusDocuments/Master/DataScience/Data_Processing/book-exercises/chapter-10-exercises/exercise-5/exercise.R')
source('D:/YifusDocuments/Master/DataScience/Data_Processing/book-exercises/chapter-10-exercises/exercise-5/exercise.R')
source('D:/YifusDocuments/Master/DataScience/Data_Processing/book-exercises/chapter-10-exercises/exercise-5/exercise.R')
# What was the most popular female name in 2013?
#tname = table(names_2013$name)
#print(max(tname))
names_2013[names_2013$prop == max(names_2013$prop)]
max(names_2013$prop)
names_2013[names_2013$prop == max(names_2013$prop)]
# What was the most popular female name in 2013?
#tname = table(names_2013$name)
#print(max(tname))
names_2013[ max(names_2013$prop)]
names_2013 %>% filter(max(names_2013$prop))
names_2013 %>% filter(max(names_2013$prop))
filter(names_2013, max(names_2013$prop))
filter(names_2013, names_2013[names_2013$prop == max(names_2013$prop)])
# What was the most popular female name in 2013?
#tname = table(names_2013$name)
#print(max(tname))
names_2013$name[max(names_2013$prop)]
# What was the most popular female name in 2013?
#tname = table(names_2013$name)
#print(max(tname))
names_2013$name[names_2013[max(names_2013$prop)]]
# What was the most popular female name in 2013?
#tname = table(names_2013$name)
#print(max(tname))
names_2013[max(names_2013$prop)]
names_2013[max(names_2013$prop)]
# What was the most popular female name in 2013?
#tname = table(names_2013$name)
#print(max(tname))
names_2013[names_2013$prop == max(names_2013$prop)]
names_2013$prop == max(names_2013$prop)
filter(names_2013,names_2013$prop == max(names_2013$prop) )
# What was the most popular female name in 2013?
#tname = table(names_2013$name)
#print(max(tname))
names_2013$prop[names_2013$prop == max(names_2013$prop)]
subset(names_2013, names_2013$prop == max(names_2013$prop))
# What was the most popular female name in 2013?
#tname = table(names_2013$name)
#print(max(tname))
names_2013$name[names_2013$prop == max(names_2013$prop)]
res = names_year$name[names_year$prop == max(names_year$prop)]
return res
names_year = subset(names,names$year == year)
res = names_year$name[names_year$prop == max(names_year$prop)]
return res
return res
# Write a function `most_popular_in_year` that takes in a year as a value and
# returns the most popular name in that year
most_popular_in_year = function(year)
{
names_year = subset(names,names$year == year)
res = names_year$name[names_year$prop == max(names_year$prop)]
return res
}
# What was the most popular female name in 2013?
names_2013$name[names_2013$prop == max(names_2013$prop)]
# Write a function `most_popular_in_year` that takes in a year as a value and
# returns the most popular name in that year
most_popular_in_year = function(year)
{
names_year = subset(names,names$year == year)
res = names_year$name[names_year$prop == max(names_year$prop)]
return res
}
# What was the most popular female name in 1994?
most_popular_in_year(2014)
# Write a function `most_popular_in_year` that takes in a year as a value and
# returns the most popular name in that year
most_popular_in_year = function(year)
{
names_year = subset(names,names$year == year)
res = names_year$name[names_year$prop == max(names_year$prop)]
return res
}
res = names_year$name[names_year$prop == max(names_year$prop)]
# What was the most popular female name in 1994?
most_popular_in_year(2014)
# Write a function `most_popular_in_year` that takes in a year as a value and
# returns the most popular name in that year
most_popular_in_year = function(year)
{
names_year = subset(names,names$year == year)
res = names_year$name[names_year$prop == max(names_year$prop)]
}
# What was the most popular female name in 1994?
most_popular_in_year(2014)
return names_year$name[names_year$prop == max(names_year$prop)]
# Write a function `most_popular_in_year` that takes in a year as a value and
# returns the most popular name in that year
most_popular_in_year = function(year)
{
names_year = subset(names,names$year == year)
return names_year$name[names_year$prop == max(names_year$prop)]
}
return res
return
# Write a function `most_popular_in_year` that takes in a year as a value and
# returns the most popular name in that year
most_popular_in_year = function(y)
{
names_year = subset(names,names$year == y)
res = names_year$name[names_year$prop == max(names_year$prop)]
return
}
# What was the most popular female name in 1994?
most_popular_in_year(2014)
return 1
return (res)
# Write a function `most_popular_in_year` that takes in a year as a value and
# returns the most popular name in that year
most_popular_in_year = function(y)
{
names_year = subset(names,names$year == y)
res = names_year$name[names_year$prop == max(names_year$prop)]
return (res)
}
# What was the most popular female name in 1994?
most_popular_in_year(2014)
source('D:/YifusDocuments/Master/DataScience/Data_Processing/book-exercises/chapter-10-exercises/exercise-5/exercise.R')
# What was the most popular female name in 1994?
most_popular_in_year(2014)
# What was the most popular female name in 1994?
most_popular_in_year(2014)
# Read in the female baby names data file found in the `data` folder into a
# variable called `names`. Remember to NOT treat the strings as factors!
names = read.csv("data/female_names.csv", stringsAsFactors = FALSE)
# What was the most popular female name in 1994?
most_popular_in_year(1994)
